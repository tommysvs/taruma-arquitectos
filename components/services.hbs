<section class="section">
    <h3>{{servicesTitle}}</h3>

    <div class="services-grid">
        {{#each services}}
            <div class="service-card">
                <h4>{{title}}</h4>
                <p>{{description}}</p>
            </div>
        {{/each}}

    </div>
</section>